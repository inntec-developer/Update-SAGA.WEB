import { ChkButtonsDirective } from './chk-buttons.directive';

describe('ChkButtonsDirective', () => {
  it('should create an instance', () => {
    const directive = new ChkButtonsDirective();
    expect(directive).toBeTruthy();
  });
});
